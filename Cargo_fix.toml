[package]
name = "fix_isbn_standalone"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
dirs = "5.0"
tokio = { version = "1.0", features = ["full"] }
